extends layout

block content

  h1 Log out area
  p= info

  .main.container
    p to log out click the 'Log out' button.
        form(method='post' action='/logout')
          input(type='submit' value='Log out')
